apiVersion: v1
kind: Service
metadata:
  name: sports-tournaments-api
spec:
  type: ClusterIP
  selector:
    app: sports-tournaments-api
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
